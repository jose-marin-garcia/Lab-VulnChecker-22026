# Datasources para Grafana (Prometheus)
# Sin uid para que Grafana lo asigne (evita "data source not found" en algunos entornos)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
